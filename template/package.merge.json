{
  "module": "src/now-ui/index.js",
  "scripts": {
    "dev": "snc ui-component develop --fetch-assets-from-instance --no-type-check",
    "clean": "now-sdk clean && sn-sdk-next-ui unpack --clean",
    "build": "now-sdk clean && sn-sdk-next-ui unpack --clean && snc ui-component generate-update-set --offline --no-type-check && sn-sdk-next-ui unpack && now-sdk build --skip-clean",
    "deploy": "now-sdk pack && now-sdk install"
  },
  "optionalDependencies": {
    "@servicenow/now-button": "instance"
  },
  "devDependencies": {
    "@servicenow/ui-core": "latest",
    "@servicenow/ui-renderer-snabbdom": "latest",
    "ajv": "6.12.6",
    "@babel/runtime": "7.18.9",
    "eslint": "8.50.0",
    "sass": "1.53.0"
  }
}